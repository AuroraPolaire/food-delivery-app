export const selectShops = (state) => state.delivery.shops;
export const selectProducts = (state) => state.delivery.products;
export const selectCart = (state) => state.delivery.cart;
export const selectTotal = (state) => state.delivery.total;
