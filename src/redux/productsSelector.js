export const selectSeafood = (state) => state.delivery.seafood;
export const selectDesserts = (state) => state.delivery.desserts;
export const selectVegan = (state) => state.delivery.vegan;
export const selectChicken = (state) => state.delivery.chicken;

export const selectLoading = (state) => state.delivery.loading;

export const selectDisabledButton = (state) => state.delivery.disableButton;

export const selectCart = (state) => state.delivery.cart;
export const selectStoreName = (state) => state.delivery.storeName;

export const selectTotal = (state) => state.delivery.total;

export const selectOrder = (state) => state.delivery.order;
export const selectId = (state) => state.delivery.id;
